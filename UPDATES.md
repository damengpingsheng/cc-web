# 更新说明

本包 = 上游 [cc-web](https://github.com/ZgDaniel/cc-web) `v1.5.1` + 下列改动，版本号 `1.5.1+my.18`。

改动分两类，来源不同，分开列：**一** 是本分支自己做的；**二** 是随一个本地 1.5.4
基线继承来的第三方实现，只是上游 `v1.5.1` 里没有，并非本分支原创。
实现细节、取舍与验证记录见 `CHANGELOG.md`。

## 解压后运行

```bash
npm start            # 依赖已随包提供，无需 npm install
```

默认监听 `127.0.0.1:8002`。需要局域网访问、改端口或设登录密码，
把 `.env.example` 复制成 `.env` 后按注释改；Windows 可直接双击 `start.bat`。
前提是本机已装好 Claude Code 或 Codex CLI 并登录过。

---

## 一、本分支自研

日期为功能落地日期。09-21 及以前的改动原本是一次性落盘的，09-22 才按功能重新拆成
线性提交，所以这部分的提交日期晚于实际编码日期。

### 2026-09-23

**新增**

- 流式气泡末尾常驻一组跳动的指示点：模型思考、没有工具在跑的空档，也能看出这一轮还没结束

**修复**

- 切换浏览器标签不再重建整棵消息树，运行中的长会话不再卡顿数秒
- 上下文压缩摘要、以及本轮最后一次工具调用，实时渲染的位置和形态与刷新后一致

### 2026-09-22

**新增**

- 会话内搜索（`Ctrl+F`），命中可逐条跳转，落在折叠区里会自动展开
- 输入框支持 `↑` 回溯历史指令
- 输入框左侧常驻显示当前会话的模型与思考强度，点击即可切换
- 用户与 agent 气泡旁显示分钟级时间，悬停可看带日期的完整时间
- 会话列表每项显示对应的 Claude CLI session id，可一键复制
- 自动清理 Claude 导入会话里的注入噪声（caveat、本地命令等），标题与正文都不再被污染
- 补齐本地命令卡片与压缩摘要卡片的样式，用户气泡改淡蓝底
- 桌面端可收起会话侧栏
- 工具参数按字段分区渲染，长文本各自成块
- 导入 CLI 会话的面板加搜索框
- 删除 Codex 会话时，连带清掉 subagent 派生的子线程

**修复**

- CLI 启动失败（如路径写错导致的 spawn ENOENT）只报错当前会话，不再打死整个服务
- 新建会话的工作目录会展开 `~` 并当场校验是否存在
- 代码块的 Copy 按钮在内网 HTTP（非安全上下文）下降级到 `execCommand`，不再点了没反应
- 上翻浏览历史时，流式输出不再把视口反复拽回底部

### 2026-09-17

**修复**

- `/model` 列表与终端 `claude` CLI 对不上：改为读 `settings.json` 里的 `modelPicker.options` 与 `ANTHROPIC_CUSTOM_MODEL_OPTION`
- Codex 在 local 模式下 `/model` 候选为空：回落到 `~/.codex/config.toml` 的当前模型与网关 gpt 系列，思考强度补齐 `low` / `max` / `ultra`
- Codex 会话只能导入一部分、且列表里标题全都一样：适配新版 rollout 的 `item_completed` / `UserMessage` 结构
- 导入的 Codex 会话进度落后于终端：subagent 的第二份 `session_meta` 不再冒充父会话

## 二、随本地 1.5.4 基线继承

以下功能来自一个第三方的本地 1.5.4 包，2026-09-21 把本地改动整体落盘时一并带入，
代码基本逐字保留。上游 `v1.5.1` 没有这些功能，但它们不是本分支写的。

- 通知渠道新增钉钉机器人，支持自定义关键词安全设置，可选 @所有人
- `HOST` 可配置监听地址，设为局域网 IP 即可供内网其他设备访问
- 支持自建网关与第三方中转（`ANTHROPIC_BASE_URL` / `ANTHROPIC_AUTH_TOKEN`）
- `/model` 与 `/effort` 两级联动，候选可从网关 `/v1/models` 拉取
- 从终端导入的 Claude / Codex CLI 会话与 CLI 实时同步（文件 watcher 加轮询双保险、inode 变化时原子替换）
- Codex rollout 识别 `custom_tool_call` / `custom_tool_call_output`
- 导入同步的「更短不覆盖」保护，避免读到截断内容覆盖已渲染消息
- 长会话增加「到最前 / 到最后」跳转圆钮
- 后台任务完成后广播给其他标签页与设备

其中 `/model` / `/effort` 与导入实时同步两项，只有上面括注的机制是继承来的，
本分支 2026-09-17 在其之上做过修正与扩展，见第一节。

## 来源与许可

上游是 [ZgDaniel/cc-web](https://github.com/ZgDaniel/cc-web)。第二类功能所来自的
本地 1.5.4 包没有 LICENSE 文件，`package.json` 里也没有 author / repository 字段，
原作者不明。上游仓库同样未附 LICENSE。转发或再分发前请自行确认授权。
