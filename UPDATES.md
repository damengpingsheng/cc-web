# 更新说明

本包基于上游 [cc-web](https://github.com/ZgDaniel/cc-web) `v1.5.1`，当前版本 `1.5.1+my.18`。
下面只列本分支新增与修复的内容及其加入日期；实现细节、取舍与验证记录见 `CHANGELOG.md`。

## 解压后运行

```bash
npm start            # 依赖已随包提供，无需 npm install
```

默认监听 `127.0.0.1:8002`。需要局域网访问、改端口或设登录密码，
把 `.env.example` 复制成 `.env` 后按注释改；Windows 可直接双击 `start.bat`。
前提是本机已装好 Claude Code 或 Codex CLI 并登录过。

---

## 2026-09-23

**新增**

- 流式气泡末尾常驻一组跳动的指示点：模型思考、没有工具在跑的空档，也能看出这一轮还没结束

**修复**

- 切换浏览器标签不再重建整棵消息树，运行中的长会话不再卡顿数秒
- 上下文压缩摘要、以及本轮最后一次工具调用，实时渲染的位置和形态与刷新后一致

## 2026-09-22

**新增**

- 支持自建网关与第三方中转，并接通思考强度（effort）参数
- `HOST` 可配置监听地址，设为局域网 IP 即可供内网其他设备访问
- 通知渠道新增钉钉机器人，支持自定义关键词安全设置，可选 @所有人
- `/model` 与 `/effort` 两级联动，模型候选列表从网关动态获取
- 从终端导入的 Claude / Codex CLI 会话与 CLI 实时同步，终端侧新产生的消息直接出现在浏览器里
- 自动清理 Claude 导入会话里的注入噪声（caveat、本地命令等），标题与正文都不再被污染
- 桌面端可收起会话侧栏
- 长会话增加「到最前 / 到最后」跳转圆钮
- 会话内搜索（`Ctrl+F`），命中可逐条跳转，落在折叠区里会自动展开
- 输入框支持 `↑` 回溯历史指令
- 导入 CLI 会话的面板加搜索框
- 工具参数按字段分区渲染，长文本各自成块
- 后台任务完成后广播给其他标签页与设备
- 删除 Codex 会话时，连带清掉 subagent 派生的子线程
- 补齐本地命令卡片与压缩摘要卡片的样式，用户气泡改淡蓝底
- 输入框左侧常驻显示当前会话的模型与思考强度，点击即可切换
- 会话列表每项显示对应的 Claude CLI session id，可一键复制
- 用户与 agent 气泡旁显示分钟级时间，悬停可看带日期的完整时间

**修复**

- CLI 启动失败（如路径写错导致的 spawn ENOENT）只报错当前会话，不再打死整个服务
- 新建会话的工作目录会展开 `~` 并当场校验是否存在
- 代码块的 Copy 按钮在内网 HTTP（非安全上下文）下降级到 `execCommand`，不再点了没反应
- 上翻浏览历史时，流式输出不再把视口反复拽回底部
